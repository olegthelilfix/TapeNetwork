# Git conventions

Branch and commit-message conventions for this repo live in the root `CONTRIBUTING.md`.
Follow them: work on a ticket branch (not `master`/`main`), and start commit subjects with a
bracketed `[TICKET-ID]`.

#[[file:../../CONTRIBUTING.md]]
